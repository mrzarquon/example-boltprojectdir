---
version: 5
defaults:
  datadir: variables
  data_hash: yaml_data
hierarchy:
  - name: "hostname"
    path: "hostname/%{facts.hostname}.yaml"

  - name: "datacenter"
    path: "datacenter/%{facts.datacenter}.yaml"

  - name: "common"
    path: "common.yaml"